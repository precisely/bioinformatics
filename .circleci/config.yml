version: 2
jobs:
  build:
    machine: true

    steps:

      - checkout
          
      - run: |
          docker build -t circleci:run_tests .
          docker images
          docker run -i circleci:run_tests
          echo ********************************************************************************************
          python setup.py install
          pip install mamba
          mamba convert_ancestry/test_convert_ancestry.py
          mamba test_ancestry_to_vcf.py
